# Инструкция по установке и запуску

## Быстрая установка (PowerShell)

### Вариант 1: Если проект уже на GitHub

```powershell
# 1. Клонировать репозиторий
cd $env:USERPROFILE\Desktop
git clone https://github.com/ваш-username/gemma-ai-extension.git
cd gemma-ai-extension

# 2. Запустить скрипт установки
.\INSTALL.ps1
```

### Вариант 2: Использовать текущую директорию

```powershell
# Если проект уже скачан, просто запустите:
.\INSTALL.ps1
```

### Вариант 3: Ручная установка

```powershell
# 1. Перейти в директорию проекта
cd "C:\Users\Drozd\Desktop\ох...аи\а я могу подключить свою модель аи к этому расшиирению как мозги которую я..._files"

# 2. Проверить наличие файлов
dir manifest.json, popup.html, background.js

# 3. Открыть Chrome/Edge и перейти на:
# Chrome: chrome://extensions/
# Edge:   edge://extensions/

# 4. Включить "Режим разработчика"

# 5. Нажать "Загрузить распакованное расширение"

# 6. Выбрать текущую директорию
```

## Пошаговая инструкция

### Шаг 1: Подготовка

```powershell
# Проверить, что вы в правильной директории
Get-Location

# Проверить наличие основных файлов
Test-Path manifest.json
Test-Path popup.html
Test-Path background.js
```

### Шаг 2: Создание иконок (если их нет)

```powershell
# Создать папку для иконок (если не существует)
New-Item -ItemType Directory -Path "icons" -Force

# Скачать или создать иконки:
# - icon16.png (16x16 пикселей)
# - icon48.png (48x48 пикселей)
# - icon128.png (128x128 пикселей)

# Можно использовать онлайн генератор:
# https://www.favicon-generator.org/
```

### Шаг 3: Установка в браузер

#### Chrome:

```powershell
# Открыть страницу расширений
Start-Process "chrome://extensions/"

# Или вручную:
# 1. Откройте Chrome
# 2. Перейдите на chrome://extensions/
# 3. Включите "Режим разработчика" (Developer mode)
# 4. Нажмите "Загрузить распакованное расширение" (Load unpacked)
# 5. Выберите директорию с расширением
```

#### Edge:

```powershell
# Открыть страницу расширений
Start-Process "edge://extensions/"

# Или вручную:
# 1. Откройте Edge
# 2. Перейдите на edge://extensions/
# 3. Включите "Режим разработчика" (Developer mode)
# 4. Нажмите "Загрузить распакованное расширение" (Load unpacked)
# 5. Выберите директорию с расширением
```

### Шаг 4: Настройка расширения

```powershell
# После установки:
# 1. Откройте расширение (кликните на иконку)
# 2. Нажмите на кнопку настроек (шестеренка)
# 3. Введите URL вашего API:
#    - Ollama: http://localhost:11434
#    - Другой сервер: ваш URL (например: http://localhost:8000)
```

## Проверка установки

```powershell
# Проверить, что расширение загружено
# В браузере на странице расширений должно быть видно:
# - Название: "Gemma 3 12B AI Assistant"
# - Версия: "1.0.0"
# - Статус: "Включено"
```

## Обновление расширения

```powershell
# Если вы обновили код:
# 1. В браузере на странице расширений
# 2. Нажмите кнопку обновления (🔄) рядом с расширением
# Или:
# 3. Отключите и снова включите расширение
```

## Устранение проблем

### Ошибка: "Манифест недействителен"

```powershell
# Проверить manifest.json на ошибки
Get-Content manifest.json | ConvertFrom-Json

# Убедиться, что все обязательные поля присутствуют
```

### Ошибка: "Иконки не найдены"

```powershell
# Проверить наличие иконок
Test-Path icons\icon16.png
Test-Path icons\icon48.png
Test-Path icons\icon128.png

# Если их нет, создайте или скачайте иконки
```

### Расширение не загружается

```powershell
# 1. Проверить консоль разработчика (F12)
# 2. Проверить ошибки на странице расширений
# 3. Убедиться, что все файлы на месте:
Get-ChildItem -Recurse -File | Select-Object Name, Length
```

## Полезные команды PowerShell

```powershell
# Показать все файлы проекта
Get-ChildItem -Recurse | Format-Table Name, Length

# Найти все .js файлы
Get-ChildItem -Filter "*.js" -Recurse

# Проверить размер проекта
(Get-ChildItem -Recurse | Measure-Object -Property Length -Sum).Sum / 1MB

# Открыть директорию в проводнике
explorer .

# Открыть файл в редакторе (VS Code)
code .
```

## Автоматизация

Создайте файл `setup.ps1` для автоматической установки:

```powershell
# setup.ps1
$projectPath = (Get-Location).Path
Write-Host "Установка расширения из: $projectPath"

# Проверка файлов
$files = @("manifest.json", "popup.html", "background.js")
foreach ($file in $files) {
    if (-not (Test-Path $file)) {
        Write-Error "Файл $file не найден!"
        exit 1
    }
}

Write-Host "✓ Все файлы на месте"
Write-Host ""
Write-Host "Следующие шаги:"
Write-Host "1. Откройте chrome://extensions/ или edge://extensions/"
Write-Host "2. Включите 'Режим разработчика'"
Write-Host "3. Нажмите 'Загрузить распакованное расширение'"
Write-Host "4. Выберите: $projectPath"
```

Запуск:
```powershell
.\setup.ps1
```

## Дополнительная информация

- Подробная инструкция: [SETUP.md](SETUP.md)
- Быстрый старт: [QUICK_START.md](QUICK_START.md)
- Руководство по агенту: [AGENT_GUIDE.md](AGENT_GUIDE.md)

