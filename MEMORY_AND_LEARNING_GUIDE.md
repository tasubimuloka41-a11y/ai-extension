# Руководство по памяти и обучению агента

## Обзор

Агент теперь имеет систему долгосрочной памяти и обучения, которая позволяет:

- **Помнить прошлый опыт** - все выполненные задачи сохраняются в памяти
- **Учиться на ошибках** - агент анализирует неудачные попытки и избегает повторения ошибок
- **Использовать успешные паттерны** - применяет проверенные методы из прошлого опыта
- **Восстанавливать состояние** - после перезапуска агент продолжает с того места, где остановился

## Как это работает

### 1. Сохранение опыта

После каждого выполнения задачи агент автоматически сохраняет:

- **Описание задачи** - что нужно было сделать
- **Выполненные действия** - последовательность действий
- **Результат** - успех или неудача
- **Контекст** - информация о странице, URL, элементы
- **Паттерны** - успешные и неудачные действия
- **Производительность** - время выполнения, количество шагов

### 2. Обучение на опыте

При выполнении новой задачи агент:

1. **Ищет похожие задачи** в памяти
2. **Анализирует успешные паттерны** из прошлого опыта
3. **Извлекает рекомендации** - какие действия работали лучше
4. **Избегает ошибок** - не повторяет действия, которые приводили к неудачам
5. **Адаптирует промпт** - использует знания для улучшения инструкций AI

### 3. Восстановление состояния

При перезапуске расширения:

- Восстанавливается очередь незавершенных задач
- Восстанавливается контекст (посещенные URL, скачанные файлы)
- Продолжается выполнение с того места, где остановилось

## Использование

### Автоматическое обучение

Обучение включено по умолчанию. Агент автоматически:

- Сохраняет опыт после каждой задачи
- Использует знания при выполнении похожих задач
- Улучшает свои действия на основе прошлого опыта

### Просмотр статистики памяти

1. Откройте панель агента
2. В разделе "Память и обучение" нажмите кнопку "📊"
3. Просмотрите статистику:
   - Всего опыта в памяти
   - Успешность выполнения
   - Количество уникальных задач
   - Размер памяти

### Управление памятью

#### Очистка памяти

1. Нажмите "🗑 Очистить" в разделе памяти
2. Выберите:
   - **Полная очистка** - удалить всю память
   - **Сохранить успешные** - удалить только неудачные опыты

#### Экспорт памяти

1. Нажмите "💾 Экспорт"
2. Память будет сохранена в JSON файл
3. Вы можете использовать этот файл для:
   - Резервного копирования
   - Переноса на другой компьютер
   - Анализа опыта агента

## Структура памяти

### Опыт (Experience)

```javascript
{
  id: "уникальный идентификатор",
  timestamp: "2024-01-15T10:30:00Z",
  task: {
    type: "autonomous_browser_task",
    description: "найти информацию о AI",
    url: "https://example.com"
  },
  actions: [...], // Последовательность действий
  result: {
    success: true,
    error: null
  },
  context: {
    pageInfo: {...},
    visitedUrls: [...]
  },
  performance: {
    stepsCount: 5,
    executionTime: 3000,
    successRate: 1.0
  },
  learnedPatterns: {
    successfulActions: [...],
    failedActions: [...],
    commonSelectors: {...}
  }
}
```

### Паттерны обучения

Агент извлекает следующие паттерны:

- **Успешные действия** - действия, которые привели к успеху
- **Неудачные действия** - действия, которые привели к ошибкам
- **Общие селекторы** - часто используемые селекторы элементов
- **Структуры страниц** - типичные структуры веб-страниц

## Примеры обучения

### Пример 1: Обучение на успешном опыте

**Первая попытка:**
```
Задача: "найти информацию о Python"
Действия: 
  1. Клик на поле поиска
  2. Ввод "Python"
  3. Клик на кнопку поиска
Результат: ✅ Успех
```

**Вторая попытка (похожая задача):**
```
Задача: "найти информацию о JavaScript"
Агент использует знания:
  - Рекомендация: использовать тот же селектор поля поиска
  - Рекомендация: та же последовательность действий
Результат: ✅ Успех (быстрее, т.к. использовал проверенный метод)
```

### Пример 2: Обучение на ошибках

**Первая попытка:**
```
Задача: "заполнить форму"
Действие: клик на "#submit-btn"
Результат: ❌ Ошибка - элемент не найден
```

**Вторая попытка (похожая задача):**
```
Задача: "заполнить другую форму"
Агент использует знания:
  - Избегает: клик на "#submit-btn" (это приводило к ошибке)
  - Пробует: найти кнопку по тексту "Отправить"
Результат: ✅ Успех (избежал прошлой ошибки)
```

## Восстановление после перезапуска

### Что восстанавливается

1. **Очередь задач** - незавершенные задачи продолжают выполняться
2. **Контекст** - посещенные URL, скачанные файлы
3. **Состояние агента** - текущая задача, статус выполнения

### Что НЕ восстанавливается

- Открытые вкладки браузера (они закрываются)
- Временные данные сессии
- Скриншоты (сохраняются только ссылки)

## Оптимизация памяти

### Автоматическая оптимизация

Система автоматически:

- Ограничивает размер памяти (максимум 1000 записей)
- Приоритезирует успешные опыты (они удаляются последними)
- Удаляет самые старые записи при переполнении

### Ручная оптимизация

1. Периодически очищайте память, оставляя только успешные опыты
2. Экспортируйте важные данные перед очисткой
3. Удаляйте старые или нерелевантные записи

## Статистика и метрики

### Доступные метрики

- **Всего опыта** - общее количество сохраненных задач
- **Успешных** - количество успешных выполнений
- **Неудачных** - количество неудачных попыток
- **Успешность** - процент успешных выполнений
- **Уникальных задач** - количество различных типов задач
- **Уникальных URL** - количество различных сайтов
- **Размер памяти** - объем занимаемой памяти

## Лучшие практики

### 1. Давайте четкие описания задач

Хорошо:
```
"найти информацию об искусственном интеллекте"
"заполнить форму регистрации"
"скачать документ PDF"
```

Плохо:
```
"сделай что-то"
"работа с сайтом"
```

### 2. Используйте похожие формулировки

Агент лучше находит похожие задачи, если описания похожи:

```
"найти информацию о Python" ✅
"найти информацию о JavaScript" ✅ (похоже)

"поиск Python" ❌ (другая формулировка)
```

### 3. Периодически проверяйте статистику

- Проверяйте успешность выполнения
- Анализируйте частые ошибки
- Очищайте память при необходимости

### 4. Экспортируйте важные данные

- Делайте резервные копии памяти
- Сохраняйте перед крупными изменениями
- Делитесь опытом между установками

## Ограничения

1. **Размер памяти** - ограничен 1000 записями (настраивается)
2. **Скриншоты** - не сохраняются полностью (только ссылки)
3. **Контекст браузера** - не восстанавливается полностью
4. **Производительность** - большой объем памяти может замедлить поиск

## Технические детали

### Хранение

Память хранится в `chrome.storage.local` под ключом `agentMemory`.

### Формат данных

```javascript
{
  experiences: [...],      // Массив опыта
  agentState: {...},       // Состояние агента
  learnedPatterns: {...},  // Извлеченные паттерны
  createdAt: "..."         // Дата создания
}
```

### Поиск похожих задач

Алгоритм поиска использует:
- Сравнение описаний задач (текстовое сходство)
- Сравнение доменов URL
- Приоритет успешным задачам
- Сортировка по дате (новые первыми)

## Устранение неполадок

### Память не сохраняется

- Проверьте разрешения расширения
- Убедитесь, что `chrome.storage.local` доступен
- Проверьте консоль на наличие ошибок

### Агент не использует знания

- Убедитесь, что обучение включено
- Проверьте, есть ли похожие задачи в памяти
- Убедитесь, что память инициализирована

### Память занимает много места

- Очистите память, оставив только успешные опыты
- Экспортируйте важные данные
- Уменьшите `maxMemorySize` в коде

## Расширение функциональности

Вы можете расширить систему памяти:

1. Добавить новые типы паттернов
2. Улучшить алгоритм поиска похожих задач
3. Добавить машинное обучение для предсказания действий
4. Интегрировать с внешними базами знаний

