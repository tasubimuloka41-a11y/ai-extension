# Руководство по автономному агенту

## Обзор

Автономный агент - это мощная система для автоматического выполнения задач в интернете. Агент может:

- Автоматически переходить по ссылкам
- Анализировать содержимое веб-страниц с помощью AI
- Скачивать файлы и сохранять их на Google Drive
- Выполнять поиск и анализ результатов
- Работать в фоновом режиме без вмешательства пользователя

## Типы задач

### 1. Анализ URL (`analyze_url`)

Анализирует содержимое веб-страницы с помощью AI модели.

```javascript
{
  type: 'analyze_url',
  url: 'https://example.com',
  options: {
    autoFollow: true,      // Автоматически переходить по найденным ссылкам
    maxFollow: 5,          // Максимум ссылок для перехода
    depth: 0,              // Текущая глубина (для ограничения)
    prompt: 'Кастомный промпт для анализа'
  }
}
```

### 2. Переход по ссылкам (`follow_links`)

Автоматически переходит по ссылкам на странице.

```javascript
{
  type: 'follow_links',
  startUrl: 'https://example.com',
  maxLinks: 10,
  depth: 0
}
```

### 3. Скачивание файла (`download_file`)

Скачивает файл по URL и сохраняет на Google Drive.

```javascript
{
  type: 'download_file',
  url: 'https://example.com/file.pdf',
  options: {
    fileName: 'custom-name.pdf',  // Опциональное имя файла
    saveToDrive: true              // Сохранить на Google Drive
  }
}
```

### 4. Поиск и анализ (`search_and_analyze`)

Выполняет поиск и анализирует результаты.

```javascript
{
  type: 'search_and_analyze',
  query: 'искусственный интеллект',
  options: {
    maxResults: 5  // Количество результатов для анализа
  }
}
```

### 5. Извлечение данных (`extract_data`)

Извлекает конкретные данные со страницы по CSS селекторам.

```javascript
{
  type: 'extract_data',
  url: 'https://example.com',
  selectors: {
    title: true,
    text: true,
    links: true,
    images: true,
    custom: {
      'prices': '.price',
      'titles': 'h2.title'
    }
  }
}
```

### 6. Цепочка задач (`chain`)

Выполняет несколько задач последовательно.

```javascript
{
  type: 'chain',
  tasks: [
    { type: 'analyze_url', url: 'https://example.com' },
    { type: 'download_file', url: 'https://example.com/file.pdf' },
    { type: 'extract_data', url: 'https://example.com', selectors: {...} }
  ]
}
```

## Использование через интерфейс

### Быстрые действия

В панели агента доступны кнопки быстрых действий:

1. **Анализ URL** - проанализировать конкретную страницу
2. **Переход по ссылкам** - автоматически перейти по ссылкам со страницы
3. **Поиск и анализ** - выполнить поиск и проанализировать результаты
4. **Скачать файл** - скачать файл по URL

### Программное использование

Вы можете добавить задачу программно через popup.js:

```javascript
await addAgentTask({
  type: 'analyze_url',
  url: 'https://example.com',
  options: { autoFollow: true, maxFollow: 5 }
});
```

## Статус и мониторинг

Агент автоматически обновляет статистику:
- Количество задач в очереди
- Количество посещенных URL
- Количество скачанных файлов

Статус агента отображается в реальном времени:
- 🟢 **Работает** - агент выполняет задачи
- ⚫ **Остановлен** - агент не активен

## Ограничения

- Максимальная глубина переходов: 5 уровней (настраивается в `agent.js`)
- Ограничение длины контента страницы: 5000 символов для анализа
- Максимум ссылок для извлечения: 20 на страницу
- Автоматическое ограничение глубины предотвращает бесконечные циклы

## Сохранение состояния

Агент автоматически сохраняет:
- Очередь задач
- Историю посещенных URL
- Список скачанных файлов
- Результаты анализа

Состояние сохраняется в `chrome.storage.local` и восстанавливается при перезапуске расширения.

## Примеры использования

### Пример 1: Анализ сайта с автоматическим переходом

```javascript
await addAgentTask({
  type: 'analyze_url',
  url: 'https://news.example.com',
  options: {
    autoFollow: true,
    maxFollow: 10,
    depth: 0
  }
});
```

### Пример 2: Скачивание всех PDF с страницы

```javascript
// Сначала извлечь ссылки на PDF
await addAgentTask({
  type: 'extract_data',
  url: 'https://example.com/documents',
  selectors: {
    links: true
  }
});

// Затем для каждой ссылки на PDF создать задачу скачивания
// (это можно сделать автоматически через цепочку задач)
```

### Пример 3: Поиск информации и анализ

```javascript
await addAgentTask({
  type: 'search_and_analyze',
  query: 'лучшие практики программирования',
  options: {
    maxResults: 5
  }
});
```

## Безопасность

- Агент не выполняет действия, требующие авторизации (кроме Google Drive)
- Все задачи выполняются в фоновых вкладках
- История посещений сохраняется локально
- Нет передачи данных на внешние серверы (кроме Google Drive API)

## Устранение неполадок

### Агент не запускается
- Проверьте, что модель Gemma 3 12B запущена и доступна
- Убедитесь, что URL API настроен правильно

### Задачи не выполняются
- Проверьте консоль разработчика на наличие ошибок
- Убедитесь, что у расширения есть необходимые разрешения

### Файлы не скачиваются
- Проверьте подключение Google Drive
- Убедитесь, что у расширения есть доступ к Google Drive API

## Расширение функциональности

Вы можете расширить агента, добавив новые типы задач в `agent.js`:

```javascript
case 'new_task_type':
  return await this.executeNewTask(task.params);
```

Затем добавьте обработку в метод `executeTask`.

