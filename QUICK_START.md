# Быстрый старт

## Минимальная настройка для начала работы

### Шаг 1: Создайте иконки (5 минут)

Создайте 3 PNG файла и поместите в папку `icons/`:
- `icon16.png` (16x16 пикселей)
- `icon48.png` (48x48 пикселей)  
- `icon128.png` (128x128 пикселей)

**Быстрый способ:** Используйте любой онлайн генератор иконок или создайте простые цветные квадраты с буквой "G".

### Шаг 2: Настройте Google Drive (опционально, 10 минут)

1. Откройте https://console.cloud.google.com/
2. Создайте проект
3. Включите "Google Drive API"
4. Создайте OAuth 2.0 Client ID (тип: Chrome Extension)
5. Откройте `manifest.json` и замените `YOUR_GOOGLE_CLIENT_ID` на ваш ID

**Примечание:** Если не нужен Google Drive, можете пропустить этот шаг. Расширение будет работать без него.

### Шаг 3: Установите расширение (2 минуты)

1. Откройте `chrome://extensions/` (или `edge://extensions/`)
2. Включите "Режим разработчика"
3. Нажмите "Загрузить распакованное расширение"
4. Выберите папку с расширением

### Шаг 4: Настройте модель (1 минута)

1. Откройте расширение
2. Нажмите на кнопку настроек (шестеренка)
3. Введите URL вашего API:
   - Ollama: `http://localhost:11434`
   - Другой сервер: ваш URL

### Готово! 🎉

Теперь вы можете:
- Отправлять запросы к модели Gemma 3 12B
- Загружать файлы (если подключен Google Drive)
- Открывать ссылки в панели расширения

## Автономное выполнение в браузере

Расширение поддерживает автономное выполнение задач в браузере с захватом экрана:

1. Откройте панель агента (иконка часов)
2. В разделе "Автономное выполнение в браузере" введите задачу
3. AI автоматически:
   - Захватит экран
   - Проанализирует содержимое
   - Выполнит необходимые действия (клики, ввод текста)
   - Проверит результат

**Важно:** Для работы vision анализа требуется модель с поддержкой анализа изображений.

Подробнее см. [AUTONOMOUS_BROWSER_GUIDE.md](AUTONOMOUS_BROWSER_GUIDE.md)

## Формат API

Расширение ожидает OpenAI-совместимый API:

**Endpoint:** `POST /v1/chat/completions`

**Запрос:**
```json
{
  "model": "gemma-3-12b",
  "messages": [{"role": "user", "content": "Привет"}],
  "temperature": 0.7,
  "max_tokens": 2000
}
```

**Ответ:**
```json
{
  "choices": [{
    "message": {
      "content": "Ответ модели"
    }
  }]
}
```

## Поддержка разных серверов

### Ollama
```bash
ollama serve
# URL: http://localhost:11434
```

### LM Studio
```bash
# Запустите сервер в LM Studio
# URL: http://localhost:1234
```

### Другие серверы
Убедитесь, что сервер поддерживает OpenAI-совместимый формат API.

## Помощь

Если что-то не работает:
1. Откройте консоль разработчика (F12)
2. Проверьте ошибки
3. Убедитесь, что модель запущена
4. Проверьте URL в настройках

Подробные инструкции см. в `SETUP.md` и `README.md`.

